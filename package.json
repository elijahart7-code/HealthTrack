{
  "name": "healthtrack",
  "private": true,
  "version": "1.0.0",
  "description": "HealthTrack -- barangay patient information management system (Express + Neon Postgres + React stack).",
  "scripts": {
    "dev": "concurrently -k -n API,WEB -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.4"
  }
}
